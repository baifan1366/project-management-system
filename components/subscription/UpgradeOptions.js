'use client';

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function UpgradeOptions({ userId }) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Upgrade Options</CardTitle>
      </CardHeader>
      <CardContent>
        {/* 升级选项内容 */}
      </CardContent>
    </Card>
  );
} 